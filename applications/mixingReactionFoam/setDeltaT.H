// No criterion is implemented to adapt the time-step in the PBE at the
// moment

/*if (adjustTimeStep)
{
    scalar dt = mixing.maxDeltaT().value();
    runTime.setDeltaT(min(dt, maxDeltaT));
    Info<< "deltaT = " <<  runTime.deltaT().value() << endl;
}*/

